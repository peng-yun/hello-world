'''use Pyinstaller to package .py document'''
# 1.安装Pyinstaller 
pip install Pyinstaller
成功安装之后会在python\\python3.5\\Script 文件夹下出现pyinstaller.exe，并且已经添加到环境变量中，可以直接使用
# 2.打包python 程序
cd 到待打包的python程序文件夹
pyinstaller yourpython.py 就成功了
# 3. 最后使用原生的python程序，不要使用第三方库，否则会出现打包失败的情况

update：
1. .csv 文件可以直接通过open（）函数来读取
2. file.write(string + '\n') 是写入的方法

通过直接的输入字符串
path = "C:\Users\E905937\Desktop\6号机\file\20200716_140556[256GB_100%Endurance_Fix17][45]"
通过input函数来输入字符串
path = input('input your folder names:')
一个会被转义字符所影响，另一个不会被转义字符串所影响。
